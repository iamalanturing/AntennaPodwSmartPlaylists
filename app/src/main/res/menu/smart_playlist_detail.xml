<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_regenerate_smart_playlist"
        android:icon="@drawable/ic_refresh"
        android:title="@string/smart_queue_regenerate"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_edit_smart_playlist"
        android:icon="@drawable/ic_pencil"
        android:title="@string/smart_queue_edit"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_delete_smart_playlist"
        android:icon="@drawable/ic_delete"
        android:title="@string/smart_queue_delete"
        app:showAsAction="never" />

</menu>
