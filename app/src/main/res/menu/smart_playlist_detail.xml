<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_regenerate_playlist"
        android:title="@string/smart_playlist_regenerate"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_edit_playlist"
        android:title="@string/edit_smart_playlist"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_delete_playlist"
        android:title="@string/delete_smart_playlist"
        app:showAsAction="never" />

</menu>
